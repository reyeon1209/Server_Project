//require('./init');
var crawling = require('./crawling');
var dataCrawling = require('./dataCrawling');

var crawlData = async () => {
    //var dramaList = await crawling.wikiCrawler(2015);
    //await dataCrawling(dramaList, "2015");

    //dramaList = await crawling.wikiCrawler(2016);
    //await dataCrawling(dramaList, "2016");

    //dramaList = await crawling.wikiCrawler(2017);
    //await dataCrawling(dramaList, "2017");

    //dramaList = await crawling.wikiCrawler(2018);
    //await dataCrawling(dramaList, "2018");

    //dramaList = await crawling.wikiCrawler(2019);
    //await dataCrawling(dramaList, "2019");

    //dramaList = await crawling.wikiCrawler(2020);
    //await dataCrawling(dramaList, "2020");

    //var dramaList = await crawling.wikiOnAirCrawler();
    //await dataCrawling(dramaList, "onAir");
    
    console.log("end");
}

crawlData();